# UC-3.4 Social Engagement Analytics

**Use Case ID:** UC-3.4

**Use Case Name:** Social Engagement Analytics (the Analytics dashboard).

**Actor(s):** Contributor, Moderator, Administrator — **the draft omits Moderator entirely from its actor list, then explicitly excludes Moderator in A5. Both are wrong.** `AnalyticsController` is `@PreAuthorize("isAuthenticated()")` only, with no role restriction; the frontend's `/analytics` route explicitly allows all three roles (`allowedRoles={["admin", "moderator", "contributor"]}`), and the sidebar nav item is unconditionally `visible: true`. A Moderator gets a full, network-wide analytics view — confirmed to be an even fuller access level than a Contributor's, not a blocked one.

**Precondition(s):** The actor is authenticated with an active session. At least one submission has been published (UC-3.2) to generate analytics data.

## Main Flow

1. The actor navigates to Analytics from their sidebar — confirmed, unconditionally visible to all three roles.
2. Role-scoped metrics: **the draft's role breakdown is wrong on two of three roles.**
   - **Contributor**: draft says "their own institution where enabled" and (implicitly, since it's not restated) their own submissions. Neither is accurate. There is no "enabled" toggle anywhere in the code — Analytics is unconditionally available. And scope is **institution-wide, not per-Contributor**: `MetricsAggregatorService.scopeFor()` builds an `AnalyticsScope` that does carry the Contributor's `userId`, but that field is never referenced in any SQL query in `AnalyticsRepository` — every query filters purely on `institution_id`. A Contributor's `contributorAnalytics` numbers (submitted/published/revision counts) include every submission at their institution, not just their own.
   - **Administrator**: confirmed accurate — network-wide, with an institution filter, date range, and drilldown views (Validator/Review Workload, Contributor Breakdown) that appear only once a specific institution is selected.
   - **Moderator**: **the draft's claim that Moderators don't see the Analytics nav item and can't access it by URL or API is flatly false.** `scopeFor()` has an explicit `"moderator"` branch granting network-wide scope (the same `postsByInstitution`/engagement data Admin sees), with one restriction: a Moderator is explicitly forbidden from applying the institution filter (`403` if attempted) — they always see the whole network, unfiltered, never drilled down to one institution. The real split isn't "Moderator blocked, others allowed" — it's **per-metric**: exactly two export/report metrics, `operational-health` and `ai-performance`, are Admin-only; every other metric (`posting-delay`, `content-completeness`, `posts-by-institution`, `facebook-engagement`) is available to Moderator and Contributor too. This is likely the actual source of the draft's confusion — a real, narrow "Moderator/Contributor blocked from two specific metrics" fact got generalized into "Moderator blocked from Analytics entirely."
3. Filters: institution (Admin only, backend-enforced — a Contributor gets a hard `403` if they even attempt to pass an `institutionId`, not merely a frontend that never offers the option). **Multi-select since 2026-09-23**: the Admin picks any number of institutions (none = all), sent as one comma-separated `institutionId` param that Spring binds to a `List<UUID>`; `AnalyticsScope` now carries `institutionIds` and every query filters with `institution_id IN (:institutionIds)` (including the shared `PublishSuccessRateRepository.between`). The single-institution drill-down panels (Review Workload, Contributor Breakdown) appear only when **exactly one** institution is selected, and the **Top Institution** KPI card only when two or more are in scope (all institutions, or 2+ selected) — ranking one institution against itself is meaningless. Date range — **a free date-range picker since 2026-09-23** (it used to be 4 fixed options, `7d`/`30d`/`90d`/`ytd`): the header's date button opens quick ranges (Today, This week, This month, Past 7/30/60/90 days, Year to date) beside a month calendar for picking any start/end date, plus Reset (back to Past 30 days). Past 7/30/90 days and Year to date still send the named presets; everything else is sent in the same `range` parameter as `YYYY-MM-DD..YYYY-MM-DD` (both ends inclusive, whole days in Philippine time, end capped at now, ≤366 days, no future start — `MetricsAggregatorService.customPeriod`), so summary, full report, and CSV export all accept it and the response echoes it back unchanged. The institution filter and date button now sit in the page header (the separate toolbar card and the scope/date/updated pills were removed), and — **category filter does not exist.** `AnalyticsRepository`'s own code comment states plainly: "Category filtering was removed — submissions carry no category." `Submission.category` exists as a field but is unused anywhere in Analytics. The draft's category filter is aspirational.
4. Export/report: **"formatted report" is not a separate document.** `GET /export/{metric}` is a genuine CSV download. `GET /report/{metric}` is a paginated JSON endpoint rendered as an in-app modal (`FullReportModal`) with a daily-breakdown tab and a detail-rows tab — explicitly the same underlying rows as the CSV, per the modal's own UI copy, not a distinct generated document (no PDF, no separately formatted artifact).
5. **Auto-refresh interval: the draft's "every 60 seconds" is wrong — it's every 5 minutes.** There genuinely is a frontend auto-refetch (`BACKGROUND_REFRESH_MS = 5 * 60_000`, only while the tab is visible), plus a manual Refresh button. The 60-second number that does exist in the codebase is unrelated to UI refresh — it's the backend's server-side cache TTL (`MetricsAggregatorService.summary` is `@Cacheable`, 60s, no eviction tied to any event) and a matching React Query `staleTime`, both of which govern whether a *request* gets cached data, not whether the dashboard *automatically re-requests*. The draft conflates these two different 60-second numbers into one incorrect claim about the actual refresh cadence.

## Alternative Flows

- **A1 — Insufficient Data: confirmed accurate.** Genuine per-panel empty states throughout (engagement panel, posts-by-institution chart, status donut, the report modal's daily/detail tabs, KPI tiles showing a "No data" pill) — none of these render raw errors for an empty dataset.
- **A2 — Facebook Engagement Data Delayed: confirmed accurate.** `FacebookEngagementSummaryDto.pendingCount` is a real, visible indicator ("N posts pending updated figures from Facebook"), shown in both Contributor and Admin/Moderator views, not merely computed and dropped.
- **A3 — Export Failure: fixed 2026-09-18, previously silent.** `FullReportModal`'s CSV download handler had no `catch` block, only `finally` — a failed export silently reverted the button from "Preparing Export…" back to normal with no error message and no retry affordance; the failure became an unhandled promise rejection in the console. Fixed by adding a `downloadError` state, set in a new `catch` block and cleared both at the start of each attempt and whenever the modal switches to a different metric; the footer now shows an inline error message (matching the report-loading panel's existing visual language) and the download button itself relabels to "Retry Download" while an error is active — clicking it simply re-attempts `handleDownload`.
- **A4 — Cross-Institution Access Attempt: confirmed accurate, and stronger than the draft implies.** This isn't just the frontend never offering another institution's data — the backend independently enforces it: a Contributor's institution always comes from their own JWT, never from a client-supplied parameter, and passing any `institutionId` at all as a Contributor gets an unconditional `403`, regardless of what value was sent.
- **A5 — Moderator Access Attempt: flatly false, see Main Flow step 2.** No rejection of any kind exists for a Moderator accessing Analytics — nav item, route, and every summary/most export endpoints are all open to them, with only the two admin-only metrics and the institution-filter restriction actually gating anything for that role.

## Postcondition(s)

Every actor sees accurate, role-scoped analytics: Contributor and Moderator both get real data (institution-wide and network-wide respectively, not blocked or toggled), Admin gets full network-wide data with drilldown. Filtering and export work as described — a failed export now surfaces an inline error with a retry affordance (fixed 2026-09-18) — except there's no category filter to apply regardless of role. The dashboard refreshes automatically, but on a 5-minute cadence, not 60 seconds.

## Not in the original draft at all

- **AI Feature Adoption panel** (Admin-only), explicit "insufficient data" flag below 20 events. **Reworked 2026-09-23** — two of the original three tiles could never move off 0% and the third was skewed: *Tag Correction* counted `manual_correction`/`edited`/`corrected` actions but the only tag UI (`AiClassificationButton`, never mounted — contributor tag suggestion was cut from scope) records `accepted`/`dismissed`, so the tile was removed; *Media Recommendation* counted `relevant`/`used` but the live AI Suggestions tab only ever recorded `shown` (the component that recorded `accepted`, `SimilarMediaPanel`, is unmounted), so `AiSuggestedMediaTab` now records `accepted` per add and the rate is added ÷ shown; *Caption Acceptance* divided by every caption event (dismiss/use/etc.) and Regenerate was logged twice (browser + server), so it's now applied ÷ generations (the server's one `re_generate` per generation; the browser duplicate was removed). Two features are newly tracked (V102 makes `ai_interaction_log.submission_id`/`institution_id` nullable, adds `suggested_value`, and allows `album_match`/`template_draft`; AI queries LEFT JOIN submissions and scope by `COALESCE(s.institution_id, ail.institution_id)`): **Album Auto-Match** — `AiAdoptionTrackingService` records the latest proposal per draft on each `suggest-album` call, then after a submit commits (listening to `SubmissionPendingEvent`/`FastTrackSubmissionEvent`) records one outcome per submission, `kept` if the final album is one the AI proposed (case-insensitive) else `changed`; tile = kept ÷ outcomes. **Template from Top Posts** — `generated` per draft returned, `saved` when saved (`POST /post-templates` with `source: "ai_top_posts"`); tile = saved ÷ generated. **Writing Check** (added 2026-09-24, V104 allows `proofread`) — `ProofreadController` records one `checked` row per check with its finding count in `suggested_value`, and `POST /ai/proofread/applied` records one `applied` row per fix the user applies; tile = fixes applied ÷ fixes suggested (with the check count underneath), counted toward the 20-event "insufficient data" threshold. Meaning-based Media Library search is not tracked (deliberately out of scope for now). The daily AI report's institution filter also moved into a subquery — in `WHERE` after the `LEFT JOIN` it dropped whole days that only had out-of-scope events.
- **Page Performance card** (Admin-only): live Facebook Page-level insights (reach, engagements, new follows, views) fetched directly from the Graph API per dashboard load, distinct from per-post engagement — explicitly designed to degrade gracefully (returns `null`, dashboard still renders) on any Facebook API failure or outage.
- **Validator/Review Workload panel and Contributor Breakdown table** — both Admin-only, and both gated specifically on having drilled down to one institution (submission volume, pending/in-review counts, average turnaround, queue-aging risk; individual contributor stats within that institution). Not available to Moderator at all, since Moderators can never select a single institution to drill into.
- **A hardcoded placeholder panel for non-admin roles**, worth flagging directly to whoever maintains the SRS: the "Content Quality & Compliance" panel a Contributor or Moderator sees (`OperationsAndEngagementCard`, when the admin-only `operationalHealth` block is null) shows **"Content Completeness 100%", "Watermark Valid 100%", "Publish Readiness: Optimal"** as literal hardcoded strings, not derived from real data — only "Missing Metadata" in that same panel is a genuine computed number. A Contributor or Moderator reading this panel could reasonably believe these are live metrics; they are not.
- The ±5-minute publish-accuracy window referenced inside the on-time-publication SQL ties directly to UC-3.2's postcondition — a cross-use-case dependency worth being aware of if either doc is revised independently.

## Fixed after initial verification (2026-09-18)

- **A3 (silent export failure) is fixed** — see the corrected Alternative Flow above.
- **A CSV/Formula Injection vulnerability (CWE-1236) in the export feature is fixed — not mentioned by the draft, found while looking closer at the export code.** `MetricsAggregatorService.escapeCsv()` only escaped embedded double-quotes and wrapped every value in quotes — it never neutralized a leading `=`, `+`, `-`, or `@` character, and CSV quoting doesn't stop a spreadsheet app from evaluating a cell as a formula regardless (that's based on the parsed cell's leading character, not the file's quoting). The `posts-by-institution` export/report rows genuinely include free-text, user-controlled fields — `event_title` (a Contributor's own submission title, saved with no sanitization at write time) and `contributor_name` — so a Contributor could set their title to a formula like `=HYPERLINK("http://evil.example",...)`, and it would execute when an Admin or Moderator later opened the exported CSV in Excel/Sheets/LibreOffice. Fixed by prefixing any cell value starting with one of those trigger characters (plus tab/CR) with a leading apostrophe before quoting — the standard mitigation, forcing spreadsheet apps to render the cell as literal text.

## Known limitations (not changed this pass)

- The hardcoded "Content Quality & Compliance" placeholder values for non-admin roles are a genuine misrepresentation risk but a larger product decision (build the real computation for Contributor/Moderator, or relabel the panel to make clear it's admin-only insight) rather than a quick fix — left as-is.

---

_Verified against the running code as of 2026-09-18, including the same-day A3 export-failure fix and the CSV/Formula Injection fix (`escapeCsv`). Primary sources: `AnalyticsController`, `MetricsAggregatorService` (`scopeFor`, `summary`, `export`, `report`, `assertMetricAllowed`, `resolvePeriod`, `aiPerformance`, `pagePerformance`, `escapeCsv`, `toCsv`), `AnalyticsRepository` (`submissionFilter`, `params`, `institutionFilterOptions`, `exportRows`/`reportRows`'s `event_title`/`contributor_name` columns), `frontend/src/components/layout/DashboardShell.tsx` (nav visibility), `frontend/src/app/App.tsx` (`/analytics` route `allowedRoles`), `frontend/src/features/analytics/AnalyticsDashboardPage.tsx`, `frontend/src/features/analytics/hooks/useAnalyticsSummary.ts` (`ANALYTICS_STALE_TIME_MS`, `BACKGROUND_REFRESH_MS`), `frontend/src/features/analytics/components/` (`FacebookEngagementPanel.tsx`, `PostsByInstitutionChart.tsx`, `StatusDonutChart.tsx`, `FullReportModal.tsx` (`handleDownload`, `downloadError`), `ExecutiveSummaryStrip.tsx`, `OperationsAndEngagementCard.tsx`), `frontend/src/api/analyticsApi.ts` (`downloadAnalyticsCsv`), `frontend/src/api/authApi.ts` (response interceptor), `MetricsAggregatorServiceTest.export_neutralizesCsvFormulaInjectionInFreeTextFields`._
